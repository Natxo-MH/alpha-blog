<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Navegación</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Natxo's Blog", root_path, class: "navbar-brand", id: "logo" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Artículos", articles_path %></li>
        <li><%= link_to "Usuarios", users_path %></li>
          <% if logged_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Acciones <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Nuevo Artículo", new_article_path %></li>
                <li><a href="#">Otra accion (construcción)</a></li>
                <li><a href="#">Alguna otra acción (construcción)</a></li>
                <li role="separator" class="divider"></li>
                <li><a href="#">Link Separador (construcción)</a></li>
                <li role="separator" class="divider"></li>
                <li><a href="#">Otro link Separador (construcción)</a></li>
              </ul>
            </li>
          <% end %>
             <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categorias <span class="caret"></span></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Todas las categorias", categories_path %></li>
                  <% Category.all.each do |category| %>
                  <li><%= link_to "#{category.name}", category_path(category) %></li>
                  <% end %>
                  <% if logged_in? and current_user.admin? %>
                    <li role="separator" class="divider"></li>
                    <li><%= link_to "Crea una nueva Categoría", new_category_path %></li>
                  <% end %>
                 
                </ul>
              </li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Buscar en Natxo's Blog">
        </div>
        <button type="submit" class="btn btn-default">Buscar</button>
      </form>
      <ul class="nav navbar-nav navbar-right">
        
        <% if logged_in? %>
            <li><%= link_to 'Cerrar Sesión', logout_path, method: :delete %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Tu Perfil <%= "Admin" if current_user.admin? %><span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Edita tu perfil", edit_user_path(current_user) %></li>
                <li><%= link_to "Revisa tu perfil", user_path(current_user) %></li>
                <li><a href="#">Alguna otra cosa (construccón)</a></li>
                <li role="separator" class="divider"></li>
                <li><a href="#">Link Separador (construcción)</a></li>
              </ul>
            </li>
        <% else %>
            <li><%= link_to 'Iniciar Sesión', login_path %></li>
            <li><%= link_to 'Regístrate', signup_path %></li>
        <% end %>


      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>